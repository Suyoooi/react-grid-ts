import * as React from "react";
import { render } from "react-dom";
import ReactGridExample from "./constant/reactGrid";

function App() {
  return <ReactGridExample />;
}

render(<App />, document.getElementById("root"));
